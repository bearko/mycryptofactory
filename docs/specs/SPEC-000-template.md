# SPEC-000: （題名）

> **テンプレート**: 本ファイルを複製し、ファイル名を `SPEC-<番号>-<短い題名>.md` に変更する。番号は [SPEC-INDEX.md](./SPEC-INDEX.md) で一意に管理する。

| 項目 | 値 |
|------|-----|
| ID | SPEC-000 |
| 状態 | Draft / Review / Accepted / Deprecated |
| 作成日 | YYYY-MM-DD |
| 最終更新 | YYYY-MM-DD |
| 作者 | （名前またはロール） |
| 関連 Epic | [SCRUM-XX](https://bearko.atlassian.net/browse/SCRUM-XX)（任意） |

## 1. 概要

（プレイヤー視点 or 開発者視点で、何が変わるかを 2〜5 文）

## 2. 背景と動機

（なぜこの SPEC が必要か。会話履歴のリンク、参考にした派生作・原作の機能、ユーザーフィードバックなど）

## 3. 目標と非目標

### 目標（Goals）

- …

### 非目標（Non-goals）

- …（Phase 5/6 にずらすもの、v2 へ持ち越すもの）

## 4. スコープ

### 含む

- …

### 含まない

- …

## 5. 契約と挙動

### 入力

- …（プレイヤー操作、他システムからの呼び出し）

### 出力・副作用

- …（state変化、UI更新、persist対象）

### エラー条件

- …（不正な状態、競合、null等）

### 互換性

- 後方互換: 維持 / 破壊的（マイグレーション手順を併記）
- セーブデータ互換: localStorage `mcf-save-v1` を保つ / 新キーへ移行

## 6. 非機能要件（該当するもののみ）

- 性能: …（フレームレート、ビルドサイズへの影響）
- アクセシビリティ: …（キーボード操作、aria）
- セキュリティ: …（XSS、localStorage の機微）
- ローカライゼーション: …（日本語固定 or 多言語化検討）

## 7. マージ前確認事項（PR レビューで人間が承認）

PR 本文の「マージ前確認事項」セクションに該当項目をチェックリスト化する。代表例：

- [ ] 共有型 (`src/game/types.ts`) を変更する
- [ ] 経済バランス値 (`src/data/balance.ts`) を変更する
- [ ] セーブデータ互換を破壊する
- [ ] 依存関係を追加・削除する
- [ ] 著作権関連アセットを追加する
- [ ] その他、PR レビュアの判断を求めたい論点（自由記述）

該当なしの場合も「該当なし」と明記する。AI エージェントは該当を検出しても**作業を中途で止めず**、PR 本文に警告として記載すること。

## 8. 受け入れ基準

- [ ] …
- [ ] …
- [ ] テストが追加されている（ロジック層の場合）/ Vercel Preview で動作確認できる（UI の場合）

## 9. テストと証跡

（期待するテストの種類、Vitest のケース数、E2E が必要なら手順、Vercel Preview のスクリーンショット要否）

## 10. 改訂履歴

| 日付 | 版 | 変更内容 |
|------|-----|----------|
| YYYY-MM-DD | 0.1 | 初版草案 |
